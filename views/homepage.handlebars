{#each users as |user| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h3>{{title}}</h3>
    <p>{{content}}<p>
    <p>{{user.username}} on {{createdAt}}</p>
    <a href="/post/{{id}}"></a>
    </div>
</div> 
{{/each}}